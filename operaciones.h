#ifndef OPERACIONES_H
#define OPERACIONES_H

#include <stdio.h>
#include <stdint.h>
//usamos una funcion y variables del tipo uint32_t para que sean numeros de 32 bits y sin signos para modificarlo
uint32_t restaF(uint32_t nu1, uint32_t nu2);
void ejecucion();


#endif